{
    "type": "object",
    "additionalProperties": false,
    "required": ["farm_id"],
    "properties": {
        "id": {"type": "string", "format": "uuid"},
        "farm_id": {"type": "string", "format": "uuid"},
        "farm_type": {"type": "string", "enum": ["megafarm", "local_farm", "homestead"]},
        "eggs_today": {"type": "integer", "minimum": 0},
        "eggs_week": {"type": "integer", "minimum": 0},
        "eggs_month": {"type": "integer", "minimum": 0},
        "active_hens": {"type": "integer", "minimum": 0},
        "total_hens": {"type": "integer", "minimum": 0},
        "avg_per_hen_per_week": {"type": "number", "minimum": 0}
    }
}

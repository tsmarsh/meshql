{
    "type": "object",
    "additionalProperties": false,
    "required": ["container_id", "source_type", "source_id", "eggs", "timestamp"],
    "properties": {
        "id": {"type": "string", "format": "uuid"},
        "container_id": {"type": "string", "format": "uuid"},
        "source_type": {"type": "string", "enum": ["farm", "transfer"]},
        "source_id": {"type": "string", "format": "uuid"},
        "eggs": {"type": "integer", "minimum": 1},
        "timestamp": {"type": "string"}
    }
}

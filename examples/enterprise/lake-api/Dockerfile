FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 5092

CMD ["gunicorn", "--bind", "0.0.0.0:5092", "--workers", "2", "--timeout", "120", "app:app"]

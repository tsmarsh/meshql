{
    "type": "object",
    "additionalProperties": false,
    "required": ["payment_date", "amount", "method", "status"],
    "properties": {
        "id": { "type": "string" },
        "customer_id": { "type": "string" },
        "bill_id": { "type": "string" },
        "legacy_bill_id": { "type": "integer" },
        "payment_date": { "type": "string" },
        "amount": { "type": "number" },
        "method": { "type": "string", "enum": ["web", "electronic", "check", "cash", "phone"] },
        "confirmation_number": { "type": "string" },
        "status": { "type": "string", "enum": ["success", "failed", "refunded"] }
    }
}
